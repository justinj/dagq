[workspace]
members = [".", "vocab"]

[package]
name = "dagq"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4"
jj-lib = { version = "0.43", default-features = false }
smallvec = "1"
vocab = { path = "vocab" }

[dev-dependencies]
criterion = "0.5"
insta = "1"
